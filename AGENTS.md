# 项目开发约束

- 后端已全部迁移到 TypeScript；禁止重新引入 Java 兼容代理、Spring Boot、Maven 或第二个写业务数据的后端。
- `index.html` 是现有页面源；后端改动不顺便重新设计页面。
- 保留既有 API 请求/响应/状态码、multipart 和 SSE 语义。真实部署、删除、push 和 MR 提交不得自动重试。
- TypeScript 使用 strict 和运行时输入校验，禁止用 `any` 或双重类型断言掩盖接口错误。
- 外部程序只能由后端预定义参数启动；不得增加接受任意命令的 HTTP 端点。
- 日志不得包含请求体、账号密码或其它凭据；路径必须规范化并校验边界。
- SQLite 是业务数据唯一运行时存储；数据库由单进程独占，未完成外部任务在重启后标为需人工处理，禁止自动重放。
- 提交前运行根目录 `npm run check`，以及前端测试、类型检查和生产构建。真实 SSH、Kubernetes、Helm、Pi、CodeHub 和 Windows 启动脚本如未在目标网络验证，必须明确说明。
